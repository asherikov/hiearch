on:
    push:
        branches:
            - '**'
        tags:
            - 'NEVER'
    pull_request:
        types: [opened, reopened]

env:
    APT_INSTALL: apt install -y --no-install-recommends

jobs:
    jammy_test:
        runs-on: ubuntu-22.04

        steps:
            - uses: actions/checkout@v5
            - run: make install_deps_apt
            - run: make install
            - run: make test
            - run: cd generators/hiearch_dinit && make install_deps_apt && make install && make test

    noble_test:
        runs-on: ubuntu-24.04

        steps:
            - uses: actions/checkout@v5
            - run: make install_deps_apt
            - run: make install
            - run: env "PATH=${HOME}/.local/bin:${PATH}" make test
            - run: cd generators/hiearch_dinit && make install_deps_apt && make install && env "PATH=${HOME}/.local/bin:${PATH}" make test

    resolute_test:
        runs-on: ubuntu-26.04

        steps:
            - uses: actions/checkout@v5
            - run: make install_deps_apt
            - run: make install
            - run: env "PATH=${HOME}/.local/bin:${PATH}" make test
            - run: cd generators/hiearch_dinit && make install_deps_apt && make install && env "PATH=${HOME}/.local/bin:${PATH}" make test

    static_checks:
        uses: asherikov/ccws/.github/workflows/reusable_test.yaml@master
        with:
            distro: resolute
            workspace_version: ""
            package_github_repo: ${{ github.repository }}
            package_version: ${{ github.sha }}
            profiles: '["static_checks"]'
